rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Pilot default: deny all until Authentication + role rules are implemented.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
